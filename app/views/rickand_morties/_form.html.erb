<%= form_with(model: rickand_morty, local: true) do |form| %>
  <% if rickand_morty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rickand_morty.errors.count, "error") %> prohibited this rickand_morty from being saved:</h2>

      <ul>
      <% rickand_morty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :rickand_morty_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
